#include <stdio.h>
#include <vector>
#include "lldb.hh"

bool LinkedListDbTable::show(int row) const{
        return false;
}

bool LinkedListDbTable::add(movie&){
        return false;
}

bool LinkedListDbTable::remove(unsigned long) {
        return false;
}
movie * LinkedListDbTable::get(unsigned int) {
        return nullptr;
}
